import { HighlightFavorite } from './highlight-favorite';

describe('HighlightFavorite', () => {
  it('should create an instance', () => {
    const directive = new HighlightFavorite();
    expect(directive).toBeTruthy();
  });
});
